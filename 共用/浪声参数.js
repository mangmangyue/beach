/* 海浪声参数 —— Iris 在 `海浪声实验室.html` v2 里调的（2026-08-26）。
 *
 * **这是纯数据。** 在实验室里拖滑块 → 复制 JSON → 整段替换下面这一块，不用碰引擎代码。
 * 引擎在 `共用/audio.js` 第 4.5 节，是照实验室里那份逐层抄过来的。
 *
 * 六层（实验室里每层都写了"为什么"）：
 *   bubble 气泡水 —— 音高上滑的极短音。真正的水声不是"沙沙"，是成千上万个小气泡
 *   grain  沙粒   —— 极短的高频颗粒，和沙滩的细闪同一种质地
 *   harp   琴音   —— 浪涌上来时弹一串五声音阶。不真实，但这个网站本来就不真实
 *   pad    气息   —— 一团很轻的和声随浪起落，负责"厚度"，替代低频轰隆
 *   chime  风铃   —— **不跟着浪**，自己随机响，让两次浪之间不死寂
 *   bed    底噪   —— 滤过的噪音。**上一版难听的元凶**，留一点点当空气感就好
 *
 * ⚠️ `master.gap` 在实验室里是"两次浪相隔几秒"，**放进场景后它不起作用** ——
 *    什么时候拍岸由 `waves.js` 的动画决定（周期在面板「浪 · 湿沙 → 涨退周期」）。
 *    留着这个字段只是为了和实验室的 JSON 对得上，整段粘贴不用删东西。
 */
export const WAVE_SOUND = {
  "master": {
    "vol": 0.55,
    "gap": 6.5,
    "atk": 1.1,
    "rel": 3.4,
    "rv": 1.6,
    "rvm": 0.3,
    "key": 196
  },
  "layers": {
    "bubble": {
      "gain": 0.5,
      "dens": 34,
      "pit": 900
    },
    "grain": {
      "gain": 0.46,
      "dens": 70,
      "tone": 5200
    },
    "harp": {
      "gain": 0.32,
      "cnt": 7,
      "spd": 80
    },
    "pad": {
      "gain": 0.33,
      "det": 6,
      "tone": 1100
    },
    "chime": {
      "gain": 0.1,
      "rate": 3.5,
      "pit": 1.5
    },
    "bed": {
      "gain": 0.02,
      "lp": 1400,
      "hp": 180
    }
  }
};

/* ---------------------------------------------------------------------------
 * 真实录音那一版的参数（`试听_真实浪.html` 里 Iris 试听过的默认值）。
 * 素材：`浪声素材/wave1..4.mp3` —— 从一段 Pixabay 录音里切的 4 个单发浪，各约 3.9 秒。
 * ⚠️ 上线前去 Pixabay 原页面确认一次许可，并在项目里记一笔来源。
 *
 * 做法是游戏音效的标准套路：4 个素材随机取一个 + 随机变调 + 随机音量声像，
 * 听不出重复。**不要循环播放整段录音** —— 那样它会自顾自地响，和画面上的浪对不上。
 * ------------------------------------------------------------------------- */
export const WAVE_REAL = {
  vol: 0.06,     // 总音量（Iris 在 试听_真实浪.html 里调的）
  gain: 0.7,     // 浪声层（实验室里的 wv）
  pitch: 0.12,   // 随机变调 ±12%（pt）
  lp: 6000,      // 高切
  rvm: 0.04,     // 混响量
  rv: 1.6,       // 混响长度（秒）
  // hp2（琴音层）= 0 —— **纯真实浪不要任何合成层**（Iris 2026-08-27）。
  // gap 在场景里不起作用，什么时候拍岸由 waves.js 的动画决定。
  files: ['wave1.mp3', 'wave2.mp3', 'wave3.mp3', 'wave4.mp3'],
};

/* ⚠️ 两版的音量都是**实验室里直连 destination 时**调的。
 * 进了场景之后还要过 ambienceBus（面板「浪声音量」）× 主音量，
 * 所以听上去会比实验室里轻一档 —— 用面板那个滑块补回来，别改这里的数。 */

export default WAVE_SOUND;
